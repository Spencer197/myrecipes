<%= render 'shared/page_title', title: "Chef #{ @chef.chefname }'s Profile Page" %>
<div class= "row">
  <div class="col-md-4 col-md-offset-5 center">
    <%= gravatar_for @chef, size: 150 %>
  </div>
</div>

<h4 align="center"><%=@chef.chefname %></h4><%#Shows the chef's name under the gravatar image.%>
<div align="center">
  <%= will_paginate @chef_recipes %><%# Calls will_paginate method for chefs index data. Adds page selector at top of page.%>
</div>
<%= render 'recipes/recipe', obj: @chef_recipes %><%# Renders _recipe.html.erb partial using its path, then defines the object to be used to render a chef's recipes.%>
<div align="center">
  <%= will_paginate @chef_recipes %><%# Calls will_paginate method for chefs index data.  Adds page selector at bottom of page.%>
</div>